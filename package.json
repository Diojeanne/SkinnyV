{
  "name": "skinnyv",
  "version": "0.1.0",
  "description": "Lean system audio visualizer",
  "scripts": {
    "dev": "echo 'no dev server needed'",
    "build": "node -e \"require('fs').mkdirSync('dist',{recursive:true});require('fs').copyFileSync('index.html','dist/index.html')\""
  }
}
